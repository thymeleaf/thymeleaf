%TEMPLATE_MODE HTML
# ------------------------------------------------------------
%CONTEXT
items = { 'een', 'twee', 'drei' }
# ------------------------------------------------------------
%INPUT
<img ctxvar:setvar="one" th:each="i : ${items}" th:id="${i}">
# ------------------------------------------------------------
%OUTPUT
<img id="een"><img id="twee"><img id="drei">

