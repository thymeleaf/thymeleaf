%EXTENDS ../../html5.01.common
# ------------------------------------------------------------
%INPUT

<p th:text="${#dates} ? 'dates exists' : 'dates does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Dates@class.name == (#dates.class.name)}">...</p>

<p th:text="${#calendars} ? 'calendars exists' : 'calendars does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Calendars@class.name == (#calendars.class.name)}">...</p>

<p th:text="${#numbers} ? 'numbers exists' : 'numbers does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Numbers@class.name == (#numbers.class.name)}">...</p>

<p th:text="${#strings} ? 'strings exists' : 'strings does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Strings@class.name == (#strings.class.name)}">...</p>

<p th:text="${#objects} ? 'objects exists' : 'objects does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Objects@class.name == (#objects.class.name)}">...</p>

<p th:text="${#bools} ? 'bools exists' : 'bools does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Bools@class.name == (#bools.class.name)}">...</p>

<p th:text="${#arrays} ? 'arrays exists' : 'arrays does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Arrays@class.name == (#arrays.class.name)}">...</p>

<p th:text="${#lists} ? 'lists exists' : 'lists does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Lists@class.name == (#lists.class.name)}">...</p>

<p th:text="${#sets} ? 'sets exists' : 'sets does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Sets@class.name == (#sets.class.name)}">...</p>

<p th:text="${#maps} ? 'maps exists' : 'maps does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Maps@class.name == (#maps.class.name)}">...</p>

<p th:text="${#aggregates} ? 'aggregates exists' : 'aggregates does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Aggregates@class.name == (#aggregates.class.name)}">...</p>

<p th:text="${#messages} ? 'messages exists' : 'messages does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Messages@class.name == (#messages.class.name)}">...</p>

<p th:text="${#ids} ? 'ids exists' : 'ids does not exist'">...</p>
<p th:text="${@org.thymeleaf.expression.Ids@class.name == (#ids.class.name)}">...</p>

# ------------------------------------------------------------
%OUTPUT

<p>dates exists</p>
<p>true</p>

<p>calendars exists</p>
<p>true</p>

<p>numbers exists</p>
<p>true</p>

<p>strings exists</p>
<p>true</p>

<p>objects exists</p>
<p>true</p>

<p>bools exists</p>
<p>true</p>

<p>arrays exists</p>
<p>true</p>

<p>lists exists</p>
<p>true</p>

<p>sets exists</p>
<p>true</p>

<p>maps exists</p>
<p>true</p>

<p>aggregates exists</p>
<p>true</p>

<p>messages exists</p>
<p>true</p>

<p>ids exists</p>
<p>true</p>

