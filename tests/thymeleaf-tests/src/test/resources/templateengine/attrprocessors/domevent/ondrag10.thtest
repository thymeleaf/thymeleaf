%TEMPLATE_MODE HTML5
# ------------------------------------------------------------
%CONTEXT
one = 2
two = 'two'
three = 'two "plus" one'
# ------------------------------------------------------------
%INPUT
<div th:ondrag="[['hello']]">..</div>
<div th:ondrag="[[${null}]]">..</div>
<div th:ondrag="[[${one}]]">..</div>
<div th:ondrag="[[${two}]]">..</div>
<div th:ondrag="[[${three}]]">..</div>
<div th:ondrag="[('hello')]">..</div>
<div th:ondrag="[(${null})]">..</div>
<div th:ondrag="[(${one})]">..</div>
<div th:ondrag="[(${two})]">..</div>
<div th:ondrag="[(${three})]">..</div>
<div th:ondrag="a${a}a[['hello']]a${a}a">..</div>
<div th:ondrag="a${a}a[[${null}]]a${a}a">..</div>
<div th:ondrag="a${a}a[[${one}]]a${a}a">..</div>
<div th:ondrag="a${a}a[[${two}]]a${a}a">..</div>
<div th:ondrag="a${a}a[[${three}]]a${a}a">..</div>
<div th:ondrag="a${a}a[('hello')]a${a}a">..</div>
<div th:ondrag="a${a}a[(${null})]a${a}a">..</div>
<div th:ondrag="a${a}a[(${one})]a${a}a">..</div>
<div th:ondrag="a${a}a[(${two})]a${a}a">..</div>
<div th:ondrag="a${a}a[(${three})]a${a}a">..</div>

<div ondrag="old" th:ondrag="[['hello']]">..</div>
<div ondrag="old" th:ondrag="[[${null}]]">..</div>
<div ondrag="old" th:ondrag="[[${one}]]">..</div>
<div ondrag="old" th:ondrag="[[${two}]]">..</div>
<div ondrag="old" th:ondrag="[[${three}]]">..</div>
<div ondrag="old" th:ondrag="[('hello')]">..</div>
<div ondrag="old" th:ondrag="[(${null})]">..</div>
<div ondrag="old" th:ondrag="[(${one})]">..</div>
<div ondrag="old" th:ondrag="[(${two})]">..</div>
<div ondrag="old" th:ondrag="[(${three})]">..</div>
<div ondrag="old" th:ondrag="a${a}a[['hello']]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[[${null}]]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[[${one}]]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[[${two}]]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[[${three}]]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[('hello')]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[(${null})]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[(${one})]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[(${two})]a${a}a">..</div>
<div ondrag="old" th:ondrag="a${a}a[(${three})]a${a}a">..</div>
# ------------------------------------------------------------
%OUTPUT
<div ondrag="&quot;hello&quot;">..</div>
<div ondrag="null">..</div>
<div ondrag="2">..</div>
<div ondrag="&quot;two&quot;">..</div>
<div ondrag="&quot;two \&quot;plus\&quot; one&quot;">..</div>
<div ondrag="hello">..</div>
<div>..</div>
<div ondrag="2">..</div>
<div ondrag="two">..</div>
<div ondrag="two &quot;plus&quot; one">..</div>
<div ondrag="a${a}a&quot;hello&quot;a${a}a">..</div>
<div ondrag="a${a}anulla${a}a">..</div>
<div ondrag="a${a}a2a${a}a">..</div>
<div ondrag="a${a}a&quot;two&quot;a${a}a">..</div>
<div ondrag="a${a}a&quot;two \&quot;plus\&quot; one&quot;a${a}a">..</div>
<div ondrag="a${a}ahelloa${a}a">..</div>
<div ondrag="a${a}aa${a}a">..</div>
<div ondrag="a${a}a2a${a}a">..</div>
<div ondrag="a${a}atwoa${a}a">..</div>
<div ondrag="a${a}atwo &quot;plus&quot; onea${a}a">..</div>

<div ondrag="&quot;hello&quot;">..</div>
<div ondrag="null">..</div>
<div ondrag="2">..</div>
<div ondrag="&quot;two&quot;">..</div>
<div ondrag="&quot;two \&quot;plus\&quot; one&quot;">..</div>
<div ondrag="hello">..</div>
<div>..</div>
<div ondrag="2">..</div>
<div ondrag="two">..</div>
<div ondrag="two &quot;plus&quot; one">..</div>
<div ondrag="a${a}a&quot;hello&quot;a${a}a">..</div>
<div ondrag="a${a}anulla${a}a">..</div>
<div ondrag="a${a}a2a${a}a">..</div>
<div ondrag="a${a}a&quot;two&quot;a${a}a">..</div>
<div ondrag="a${a}a&quot;two \&quot;plus\&quot; one&quot;a${a}a">..</div>
<div ondrag="a${a}ahelloa${a}a">..</div>
<div ondrag="a${a}aa${a}a">..</div>
<div ondrag="a${a}a2a${a}a">..</div>
<div ondrag="a${a}atwoa${a}a">..</div>
<div ondrag="a${a}atwo &quot;plus&quot; onea${a}a">..</div>
