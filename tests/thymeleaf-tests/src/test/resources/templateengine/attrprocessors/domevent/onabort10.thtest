%TEMPLATE_MODE HTML5
# ------------------------------------------------------------
%CONTEXT
one = 2
two = 'two'
three = 'two "plus" one'
# ------------------------------------------------------------
%INPUT
<div th:onabort="[['hello']]">..</div>
<div th:onabort="[[${null}]]">..</div>
<div th:onabort="[[${one}]]">..</div>
<div th:onabort="[[${two}]]">..</div>
<div th:onabort="[[${three}]]">..</div>
<div th:onabort="[('hello')]">..</div>
<div th:onabort="[(${null})]">..</div>
<div th:onabort="[(${one})]">..</div>
<div th:onabort="[(${two})]">..</div>
<div th:onabort="[(${three})]">..</div>
<div th:onabort="a${a}a[['hello']]a${a}a">..</div>
<div th:onabort="a${a}a[[${null}]]a${a}a">..</div>
<div th:onabort="a${a}a[[${one}]]a${a}a">..</div>
<div th:onabort="a${a}a[[${two}]]a${a}a">..</div>
<div th:onabort="a${a}a[[${three}]]a${a}a">..</div>
<div th:onabort="a${a}a[('hello')]a${a}a">..</div>
<div th:onabort="a${a}a[(${null})]a${a}a">..</div>
<div th:onabort="a${a}a[(${one})]a${a}a">..</div>
<div th:onabort="a${a}a[(${two})]a${a}a">..</div>
<div th:onabort="a${a}a[(${three})]a${a}a">..</div>

<div onabort="old" th:onabort="[['hello']]">..</div>
<div onabort="old" th:onabort="[[${null}]]">..</div>
<div onabort="old" th:onabort="[[${one}]]">..</div>
<div onabort="old" th:onabort="[[${two}]]">..</div>
<div onabort="old" th:onabort="[[${three}]]">..</div>
<div onabort="old" th:onabort="[('hello')]">..</div>
<div onabort="old" th:onabort="[(${null})]">..</div>
<div onabort="old" th:onabort="[(${one})]">..</div>
<div onabort="old" th:onabort="[(${two})]">..</div>
<div onabort="old" th:onabort="[(${three})]">..</div>
<div onabort="old" th:onabort="a${a}a[['hello']]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[[${null}]]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[[${one}]]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[[${two}]]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[[${three}]]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[('hello')]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[(${null})]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[(${one})]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[(${two})]a${a}a">..</div>
<div onabort="old" th:onabort="a${a}a[(${three})]a${a}a">..</div>
# ------------------------------------------------------------
%OUTPUT
<div onabort="&quot;hello&quot;">..</div>
<div onabort="null">..</div>
<div onabort="2">..</div>
<div onabort="&quot;two&quot;">..</div>
<div onabort="&quot;two \&quot;plus\&quot; one&quot;">..</div>
<div onabort="hello">..</div>
<div>..</div>
<div onabort="2">..</div>
<div onabort="two">..</div>
<div onabort="two &quot;plus&quot; one">..</div>
<div onabort="a${a}a&quot;hello&quot;a${a}a">..</div>
<div onabort="a${a}anulla${a}a">..</div>
<div onabort="a${a}a2a${a}a">..</div>
<div onabort="a${a}a&quot;two&quot;a${a}a">..</div>
<div onabort="a${a}a&quot;two \&quot;plus\&quot; one&quot;a${a}a">..</div>
<div onabort="a${a}ahelloa${a}a">..</div>
<div onabort="a${a}aa${a}a">..</div>
<div onabort="a${a}a2a${a}a">..</div>
<div onabort="a${a}atwoa${a}a">..</div>
<div onabort="a${a}atwo &quot;plus&quot; onea${a}a">..</div>

<div onabort="&quot;hello&quot;">..</div>
<div onabort="null">..</div>
<div onabort="2">..</div>
<div onabort="&quot;two&quot;">..</div>
<div onabort="&quot;two \&quot;plus\&quot; one&quot;">..</div>
<div onabort="hello">..</div>
<div>..</div>
<div onabort="2">..</div>
<div onabort="two">..</div>
<div onabort="two &quot;plus&quot; one">..</div>
<div onabort="a${a}a&quot;hello&quot;a${a}a">..</div>
<div onabort="a${a}anulla${a}a">..</div>
<div onabort="a${a}a2a${a}a">..</div>
<div onabort="a${a}a&quot;two&quot;a${a}a">..</div>
<div onabort="a${a}a&quot;two \&quot;plus\&quot; one&quot;a${a}a">..</div>
<div onabort="a${a}ahelloa${a}a">..</div>
<div onabort="a${a}aa${a}a">..</div>
<div onabort="a${a}a2a${a}a">..</div>
<div onabort="a${a}atwoa${a}a">..</div>
<div onabort="a${a}atwo &quot;plus&quot; onea${a}a">..</div>
